<%= form_with(model: Postcode, local: true) do |form| %>
  <% if postcode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(postcode.errors.count, "error") %> prohibited this postcode from being saved:</h2>

      <ul>
        <% postcode.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
